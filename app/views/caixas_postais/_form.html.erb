<%= form_for(@caixa_postal) do |f| %>
  <% if @caixa_postal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@caixa_postal.errors.count, "error") %> prohibited this caixa_postal from being saved:</h2>

      <ul>
      <% @caixa_postal.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descricao %><br />
    <%= f.text_field :descricao %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
